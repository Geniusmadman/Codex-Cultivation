# 银月灵宠当前状态与验收项

更新时间：2026-07-22（Windows 运行快照仍为 2026-07-20）

## 结论

银月已经具备五套独立的境界图集，素材装配和 Pet v2 资源校验已完成；Windows 与 macOS 均已接入安装、境界同步、宠物辅助窗口热重载、待重启提示和安全恢复链路。两端共用同一套五境资源契约，但各自使用平台原生的状态目录、启动器和托盘/菜单栏控制。

自动化回归覆盖了灵宠管理器、五境切换、主窗口排除、加载资源验证、控制器参数和安全移除。正式发布前仍建议分别完成 Windows 与 macOS 的可见窗口五境播放验收；这项人工视觉验收不影响当前代码链路可用。

炼气继续使用原有正式图集；筑基使用双尾素材，金丹使用三尾素材，元婴和化神使用狐耳人形素材。五个资源文件的 SHA-256 已经互不相同。

## 最近一次 Windows 运行状态

本次核对时的本机状态如下：

- Codex Cultivation 会话已经启动，使用回环地址端口 `9335`，CDP Browser ID 和注入器状态均存在。
- 原生 Pet v2 灵宠 `yinyue` 已安装到 `%USERPROFILE%\.codex\pets\yinyue`。
- 当前境界为 `qi`，活动资源为 `spritesheet-qi-f57587439aa0.webp`。
- `pendingReload` 为 `false`，当前炼气资源的加载已经得到验证。
- 管理状态位于 `%LOCALAPPDATA%\CodexCultivation\pet-state.json`。
- 当前活动宠物目录已经收敛为 `pet.json` 和一个炼气图集，没有遗留待清理的境界版本。

## 已完成

### Pet v2 资源基础

- 固定宠物 ID 为 `yinyue`，显示名称为“银月”。
- 使用 `spriteVersionNumber: 2`。
- 炼气图集为 `1536 x 2288` WebP，采用 `8 x 11` 单元格布局。
- 炼气图集通过 Pet v2 尺寸、透明度、动画行、观察方向和资源格式校验。
- `pet-family.json` 已声明炼气、筑基、金丹、元婴、化神五个境界。

### 安装与状态管理

- 已实现 `verify`、`install`、`set-realm`、`remove` 四个管理命令。
- 已实现基于境界和 SHA-256 前 12 位的版本化图集文件名。
- 已实现独立的 `pet-state.json`，不修改修仙界面运行状态 schema。
- 已实现同 ID 非托管目录拒绝覆盖、文件哈希校验、原子写入和安全删除。
- 删除和恢复只处理能够由管理状态和哈希证明属于银月的文件，不扫描或修改其他宠物。
- Windows 安装脚本默认安装银月，并支持 `-NoSpiritPet`；恢复支持 `-KeepSpiritPet`。
- macOS 安装脚本默认安装银月，并支持 `--no-spirit-pet`；恢复支持 `--keep-spirit-pet`。
- macOS 的灵宠状态位于 `~/Library/Application Support/CodexCultivation/pet-state.json`，宠物安装到 `${CODEX_HOME:-~/.codex}/pets/yinyue`。
- 禁用灵宠管理只写入平台状态目录中的禁用标记，不删除已经存在的银月。

### 境界联动

- 注入器能够读取修仙系统当前大境界。
- 境界检查频率限制为最多每 2 秒一次。
- 境界未变化时不会重复写入宠物文件。
- 只对 `data-avatar-id="yinyue"` 的宠物辅助窗口执行定向重载，不重载 Codex 主窗口。
- 重载后会验证实际加载的版本化图集 URL；无法确认时保留 `pendingReload`。
- Windows 托盘和 macOS 菜单栏均能够显示当前银月境界、手动同步境界，并在需要时提示重启 Codex。
- macOS 只重载 `app://`、包含 `data-avatar-id="yinyue"` 且不具有主 Codex shell 标记的辅助窗口。
- 所有宠物相关重启路径都保留显式用户确认。

### 已有验证证据

- 五个境界目录中的独立图集均通过 Pet v2 确定性校验。
- 灵宠管理器已在真实本机目录完成炼气、筑基、金丹、元婴、化神、炼气的完整切换循环。
- 每次切换均生成正确的境界化版本文件名，并被 `verify` 识别。
- 灵宠管理、CDP 窗口分类、重载验证、安装和恢复接线专项测试通过。
- Windows 完整回归通过，最终退出码为 `0`。
- macOS 回归覆盖了灵宠管理、CDP 窗口分类、控制器安装/禁用/保留/同步参数和菜单栏重启确认接线。
- 新版 Codex 的 `[desktop.appearanceLightChromeTheme.*]` 配置结构已兼容，并使用真实配置副本验证安装和恢复能够字节级往返。

## 当前境界资源

| 境界 | 目录 | 当前资源性质 | 外观状态 |
| --- | --- | --- | --- |
| 炼气 `qi` | `forms/qi` | 正式炼气图集 | 幼态单尾银白月狐 |
| 筑基 `foundation` | `forms/foundation` | 独立双尾图集 | 双尾银月与青玉毛纹 |
| 金丹 `golden-core` | `forms/golden-core` | 独立三尾图集 | 三尾银月与暖金金丹月印 |
| 元婴 `nascent-soul` | `forms/nascent-soul` | 独立五尾图集 | 狐耳人形、五尾与银蓝紫仙袍 |
| 化神 `transformation` | `forms/transformation` | 独立九尾图集 | 狐耳人形、层叠九尾与高阶仙袍 |

## 剩余验收项

### 已完成：四套正式境界图集

筑基、金丹、元婴、化神已经分别装配为独立的完整 Pet v2 图集，不再复用炼气文件。四套资源均满足 `1536 x 2288`、`8 x 11`、`192 x 208` 单元格契约，并包含 9 行标准动画和 16 个观察方向。四个 `placeholderSourceRealm` 标记已经删除。

### P0：完成两端可见窗口五境验收

现有测试证明代码和文件状态链路可以工作，正式发布前仍建议在 Windows 与 macOS 各完成一次以下可见界面闭环：

- 银月宠物窗口未打开时切换境界。
- 银月宠物窗口已经打开时自动热重载。
- 从炼气依次切换到筑基、金丹、元婴、化神，并确认屏幕上的外观真实变化。
- 热重载失败时保留 `pendingReload`，托盘正确提示重启。
- 用户确认重启后加载目标境界资源并清除 `pendingReload`。
- 完整恢复默认移除银月，Windows `-KeepSpiritPet` / macOS `--keep-spirit-pet` 保留银月。
- 全程确认其他内置宠物和用户宠物的文件哈希不变。

### P1：校准文档和发布口径

README 和 Changelog 已描述“五境自动进化”，素材清单现在与该描述一致。正式发布前仍需补充 Windows 实机验收证据和最终版本说明。

正式发布说明至少需要明确：

- 五个境界均使用不同的正式资源。
- Windows 与 macOS 的可见窗口五境切换验收范围。
- macOS 已支持银月安装、自动/手动同步、菜单栏状态、待重启提示和安全恢复。
- 用户需要在 Codex 原生宠物选择器中选择一次“银月”；安装器不会强制替用户切换当前宠物。

### P1：完成 Git 交付

当前正式资源仍是本地工作区改动，没有形成最终功能提交，也没有推送。现阶段符合“本地验证、不上传”的要求，但不属于可交付完成状态。

正式提交前需要：

- 复核四套正式资源的最终接触表和 Windows 实机播放效果。
- 检查只提交项目源文件和最终资源，不提交本机状态、日志、截图、临时 QA 文件或备份。
- 重新运行完整回归和五境实机验收。
- 使用清晰的功能提交信息，并在获得明确授权后再推送。

### 已完成：macOS 灵宠支持

macOS 已实现独立的灵宠管理器与资源目录，并接入控制器、注入器和菜单栏：

- 默认安装炼气银月，支持 `--no-spirit-pet` 禁用管理而不删除宠物。
- 按当前修仙境界自动同步，也支持 `sync-pet` 和菜单栏手动同步。
- 对银月辅助窗口定向热重载并验证版本化图集 URL，主 Codex 窗口永不作为宠物窗口处理。
- 热重载无法确认时保留 `pendingReload`，菜单栏显示“重启 Codex 应用灵宠进阶”。
- 重启必须经过用户确认；恢复默认安全移除受管理银月，`--keep-spirit-pet` 可保留。

## 使用边界

- 只使用原生银月：在 Codex 宠物选择器中选择“银月”即可显示当前已安装形态。
- 使用界面与境界联动：Windows 必须通过 `Codex Cultivation` 快捷方式或 `start-codex-cultivation.ps1` 启动；macOS 使用 `start-codex-cultivation.command`。
- 当前切换到筑基、金丹、元婴、化神时，状态、资源文件名和外观都会变化。
- 两端安装器只安装和管理银月，不会自动替用户选择或打开宠物窗口。

## 完成标准

只有同时满足以下条件，才能把银月标记为“五境灵宠完成”：

- 五个境界各自拥有独立、通过校验的 Pet v2 图集。
- 四个占位标记已移除，五个图集不再复用同一文件。
- 五境自动同步、热重载、失败待重启和恢复流程完成 Windows 与 macOS 可见窗口验收。
- 其他宠物在安装、进阶和恢复前后的文件保持不变。
- README、Changelog 和实际能力一致。
- 完整自动回归通过，最终功能改动形成可审查的 Git 提交。

## 建议后续顺序

1. 完成 Windows 与 macOS 五境可见窗口验收和恢复验收。
2. 更新 Changelog 和最终版本说明。
3. 整理最终 Git 提交；是否推送由用户另行确认。
